# Dados do GeoMundo

Os arquivos desta pasta são a fonte de dados estática do projeto. A interface mantém uma base offline compacta no `app.js` e enriquece os registros com dados atualizados quando há conexão, armazenando o resultado no navegador para os acessos seguintes.
